.build/
.swiftpm/
*.xcodeproj
.DS_Store

# Release artifacts: signed binary and packed bundle are GitHub release assets,
# not sources. The mcpb/server/ directory itself is tracked so `mcpb pack` has a
# place to put the binary on a fresh clone.
dist/
mcpb/server/*
!mcpb/server/.gitkeep
mcpb/*.mcpb
mcpb/*.mcpb.sha256
